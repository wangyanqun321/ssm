<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wyq.ssm.mapper.UserMapper">
	<insert id="addUser">
		insert into user values(null,#{name},#{age})
	</insert>
	<select id="listAll" resultType="com.wyq.ssm.pojo.User">
		select * from user;
	</select>
</mapper>